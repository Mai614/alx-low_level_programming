#include "main.h"
/**
 *main - entery point
 *@argc: count of the arguments
 *@argv: an array of pointers to the strings
 *Return: always 0
 */

int main(int argc, char *argv[])
{
(void)argc;

printf("%s\n", argv[0]);

return (0);

}
